apiVersion: v1

kind: PersistentVolumeClaim

metadata:

    name: shared_pvc

spec:

    accessModes:

        -ReadWriteMany

    resources:
   
        requests:
  
            storage: 1Gi