apiVersion: v1

kind: PersistentVolumeClaim

metadata:

    name: shared-pvc-new

spec:

    storageClassName: dynamic-storage
    
    accessModes:

        - ReadWriteMany

    resources:
   
        requests:
  
            storage: 20Gi
