apiVersion: v1

kind: PersistentVolume

metadata:
    
    name: shared_pv

spec:

    capacity:

        storage: 1Gi

    accessModes:

        - ReadWriteMany
  
    hostPath:

        path: /mnt/data